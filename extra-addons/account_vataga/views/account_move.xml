<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_move_form_account" model="ir.ui.view">
        <field name="name">view.move.form.account</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form" />
        <field name="arch" type="xml">
            <xpath expr="//button[@name='button_draft']" position="attributes">
                <attribute name="confirm">Ви впевнені, що хочете перейти в чернетку? Якщо є кнопки модерації, вони будуть скинуті.</attribute>
            </xpath>
            <xpath expr="//group[@id='header_left_group']" position="inside">
                <field name="project_account_id" readonly="state != 'draft'"
                    groups="analytic.group_analytic_accounting"
                    invisible="move_type != 'in_invoice'"
                    options="{'no_create': True, 'no_open': True}" />
                <field name="budget_account_id" readonly="state not in ('draft', 'posted')"
                    groups="analytic.group_analytic_accounting"
                    invisible="move_type != 'in_invoice'"
                    options="{'no_create': True, 'no_open': True}" />
                <field name="cash_flow_item_account_id" readonly="state not in ('draft', 'posted')"
                    groups="analytic.group_analytic_accounting"
                    invisible="move_type != 'in_invoice'"
                    options="{'no_create': True, 'no_open': True}" />
                <field name="seller_contract_id" readonly="state != 'draft'"
                    groups="analytic.group_analytic_accounting"
                    invisible="move_type != 'in_invoice'"
                    options="{'no_create': True, 'no_open': True}" />
            </xpath>
            <xpath expr="//field[@name='invoice_line_ids']" position="attributes">
                <attribute name="invisible">state == 'posted' and move_type in ('in_invoice', 'out_invoice')</attribute>
            </xpath>
            <xpath expr="//field[@name='invoice_line_ids']" position="after">
                <field name="invoice_line_ids"
                    widget="section_and_note_one2many"
                    mode="tree"
                    context="{
                        'default_move_type': context.get('default_move_type'),
                        'journal_id': journal_id,
                        'default_partner_id': commercial_partner_id,
                        'default_currency_id': currency_id or company_currency_id,
                        'default_display_type': 'product',
                        'quick_encoding_vals': quick_encoding_vals,
                    }"
                    readonly="state != 'posted'"
                    invisible="state != 'posted' or move_type not in ('in_invoice', 'out_invoice')">
                    <tree editable="bottom" create="0" delete="0" string="Journal Items" default_order="sequence, id">
                        <field name="sequence" column_invisible="True" />
                        <field name="vataga_locked_analytic_plan_ids" column_invisible="True" />
                        <field name="product_id" optional="show" readonly="1" />
                        <field name="name" widget="section_and_note_text" optional="show" readonly="1" />
                        <field name="account_id"
                            groups="account.group_account_readonly"
                            optional="hide"
                            readonly="1" />
                        <field name="analytic_distribution" widget="analytic_distribution"
                            groups="analytic.group_analytic_accounting"
                            optional="show"
                            options="{'product_field': 'product_id', 'account_field': 'account_id', 'amount_field': 'price_subtotal', 'vataga_locked_plan_field': 'vataga_locked_analytic_plan_ids'}"
                            business_domain_compute="parent.move_type in ['out_invoice', 'out_refund', 'out_receipt'] and 'invoice' or parent.move_type in ['in_invoice', 'in_refund', 'in_receipt'] and 'bill' or 'general'" />
                        <field name="quantity" optional="show" readonly="1" />
                        <field name="product_uom_category_id" column_invisible="True" />
                        <field name="product_uom_id" string="UoM" groups="uom.group_uom" optional="show" readonly="1" />
                        <field name="price_unit" string="Price" readonly="1" />
                        <field name="discount" string="Disc.%" optional="hide" readonly="1" />
                        <field name="tax_ids" widget="many2many_tags" optional="show" readonly="1" />
                        <field name="price_subtotal" string="Tax excl." readonly="1" />
                        <field name="price_total" string="Tax incl." optional="hide" readonly="1" />
                        <field name="partner_id" column_invisible="True" />
                        <field name="currency_id" column_invisible="True" />
                        <field name="company_id" column_invisible="True" />
                        <field name="company_currency_id" column_invisible="True" />
                        <field name="display_type" force_save="1" column_invisible="True" />
                    </tree>
                </field>
            </xpath>
        </field>
    </record>

</odoo>
